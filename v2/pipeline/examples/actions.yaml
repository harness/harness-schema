# Pipeline with actions
pipeline:
  stages:
  - steps:
    - action:
        uses: docker-build-push
        with:
          repo: harness/myapp
          tags: latest
          push: true
    - action:
        uses: slack-notify
        with:
          channel: "#deployments"
          message: "Build completed"
