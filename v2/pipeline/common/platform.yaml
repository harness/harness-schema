title: platform
type: object
properties:
  os:
    type: string
    enum: [linux, windows, macos]
  arch:
    type: string
    enum: [amd64, arm64, arm]
"$schema": http://json-schema.org/draft-07/schema#
