title: strategy
type: object
properties:
  matrix:
    oneOf:
      - type: object
        additionalProperties:
          oneOf:
            - type: array
              items:
                oneOf:
                  - type: string
                  - type: number
                  - type: boolean
            - type: object
        description: "Matrix build strategy"
      - type: array
        items:
          type: object
  for:
    type: object
    properties:
      items:
        oneOf:
          - type: array
          - type: string
      max-concurrency:
        type: number
    description: "Loop strategy"
  while:
    type: object
    properties:
      condition:
        type: string
      max-iterations:
        type: number
    description: "While loop strategy"
  max-concurrency:
    type: number
    description: "Maximum concurrent executions"
"$schema": http://json-schema.org/draft-07/schema#
