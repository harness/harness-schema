title: StepQueue
type: object
required:
  - key
properties:
  key:
    type: string
    description: "Queue key"
  scope:
    type: string
    enum: [pipeline, stage]
    description: "Queue scope"
"$schema": http://json-schema.org/draft-07/schema#
