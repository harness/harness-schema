{
  "type" : "object",
  "title" : "overlayInputSet",
  "required" : [ "spec", "version", "kind" ],
  "properties" : {
    "version" : {
      "description" : "Version defines the schema version.",
      "type" : "number",
      "enum" : [ 1 ]
    },
    "kind" : {
      "description" : "defines the kind of yaml (pipeline/template)",
      "type" : "string",
      "enum" : [ "overlayInputSet" ]
    },
    "spec" : {
      "required" : [ "inputSetReferences" ],
      "properties" : {
        "inputSetReferences" : {
          "type" : "array",
          "items" : {
            "type" : "string"
          }
        }
      }
    }
  },
  "definitions" : { }
}