title: conditions
description: conditions for artifact trigger
properties:
  event:
    type: array
    items:
      "$ref": "../trigger_event_data.yaml"
  jexl:
    type: string
  metadata:
    type: array
    items:
      "$ref": "../trigger_event_data.yaml"