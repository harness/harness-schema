title: nexus
allOf:
  - "$ref": "./artifact_type_spec.yaml"
  - type: object
    properties:
      artifact_id:
        type: string
      classifier:
        type: string
      connector:
        type: string
      event_conditions:
        type: array
        items:
          "$ref": "../trigger_event_data.yaml"
      extension:
        type: string
      group_id:
        type: string
      jexl_condition:
        type: string
      metadata_conditions:
        type: array
        items:
          "$ref": "../trigger_event_data.yaml"
      pkg_name:
        type: string
      repo_format:
        type: string
      repo_name:
        type: string
      repo_url:
        type: string
      tag:
        type: string
"$schema": http://json-schema.org/draft-07/schema#
