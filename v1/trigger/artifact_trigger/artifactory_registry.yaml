title: artifactory_registry
allOf:
  - "$ref": "./artifact_type_spec.yaml"
  - type: object
    properties:
      dir:
        type: string
      filter:
        type: string
      path:
        type: string
      connector:
        type: string
      conditions:
        $ref: ./conditions.yaml
      repo:
        $ref: ./repo.yaml
"$schema": http://json-schema.org/draft-07/schema#
