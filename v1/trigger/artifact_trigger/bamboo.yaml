title: bamboo
allOf:
  - "$ref": "./artifact_type_spec.yaml"
  - type: object
    properties:
      paths:
        type: array
        items:
          type: string
      build:
        type: string
      connector:
        type: string
      conditions:
        $ref: ./conditions.yaml
      plan_key:
        type: string
"$schema": http://json-schema.org/draft-07/schema#
