title: bamboo
allOf:
  - "$ref": "./artifact_type_spec.yaml"
  - type: object
    properties:
      artifact_paths:
        type: array
        items:
          type: string
      build:
        type: string
      connector:
        type: string
      event_conditions:
        type: array
        items:
          "$ref": "../trigger_event_data.yaml"
      jexl_condition:
        type: string
      metadata_conditions:
        type: array
        items:
          "$ref": "../trigger_event_data.yaml"
      plan_key:
        type: string
"$schema": http://json-schema.org/draft-07/schema#
