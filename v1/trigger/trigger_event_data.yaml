title: trigger_event_data
type: object
properties:
  key:
    type: string
  operator:
    type: string
    enum:
      - in
      - equals
      - not-equals
      - not-in
      - regex
      - ends-with
      - starts-with
      - contains
      - does-not-contain
  value:
    type: string
"$schema": http://json-schema.org/draft-07/schema#
