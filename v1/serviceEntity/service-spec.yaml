title: ServiceSpec
type: object
required:
- spec
- type
properties:
  type:
    type: string
    enum:
    - kubernetes
allOf:
- if:
    properties:
      type:
        const: kubernetes
  then:
    properties:
      spec:
        $ref: kubernetes-service-spec.yaml
$schema: http://json-schema.org/draft-07/schema#