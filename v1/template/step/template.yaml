type: object
title: StepTemplate
required:
  - type
  - spec
properties:
  type:
    description: defines the type of template
    type: string
    enum:
      - step
allOf:
  - if:
      properties:
        type:
          const: step
    then:
      properties:
        spec:
          configRef: ../template_config.yaml/step_template_types