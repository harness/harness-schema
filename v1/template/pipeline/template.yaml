type: object
title: PipelineTemplate
required:
  - type
  - spec
properties:
  type:
    description: defines the type of template
    type: string
    enum:
      - pipeline
allOf:
  - if:
      properties:
        type:
          const: pipeline
    then:
      properties:
        spec:
          $ref: ./pipeline_spec.yaml
