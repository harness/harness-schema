type: object
title: PipelineTemplate
required:
  - type
  - spec
properties:
  type:
    description: defines the type of template
    type: string
    enum:
      - pipeline
  spec:
    $ref: ./pipeline_spec.yaml
#allOf:
#  - if:
#      properties:
#        type:
#          const: pipeline
#    then:
#      properties:
#        spec:
#          $ref: ./pipeline_spec.yaml
