type: object
title: StepGroupTemplate
required:
  - type
  - spec
properties:
  type:
    description: defines the type of template
    type: string
    enum:
      - group
allOf:
  - if:
      properties:
        type:
          const: group
    then:
      properties:
        spec:
          type: object