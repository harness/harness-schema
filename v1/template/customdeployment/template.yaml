type: object
title: CustomDeploymentTemplate
required:
  - type
  - spec
properties:
  type:
    description: defines the type of template
    type: string
    enum:
      - custom-deployment
  spec:
      configRef: ../template_config.yaml/custom_deployment_template_types