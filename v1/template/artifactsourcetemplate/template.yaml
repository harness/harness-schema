type: object
title: ArtifactSourceTemplate
required:
  - type
  - spec
properties:
  type:
    description: defines the type of template
    type: string
    enum:
      - artifact-source
allOf:
  - if:
      properties:
        type:
          const: artifact-source
    then:
      properties:
        spec:
          $ref: ../template_config.yaml/artifact_source_template_types
