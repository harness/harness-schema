type: object
title: StageTemplate
required:
  - type
  - spec
properties:
  type:
    description: defines the type of template
    type: string
    enum:
      - stage
allOf:
  - if:
      properties:
        type:
          const: stage
    then:
      properties:
        spec:
          configRef: ../../pipeline/schema_config.yaml/pipeline_stages