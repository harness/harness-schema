type: object
title: SecretManagerTemplate
required:
  - type
  - spec
properties:
  type:
    description: defines the type of template
    type: string
    enum:
      - secret-manager
allOf:
  - if:
      properties:
        type:
          const: secret-manager
    then:
      properties:
        spec:
          type: object
