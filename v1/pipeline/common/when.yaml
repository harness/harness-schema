title: When
description: Expression used to evaluate whether or not a Stage or Step should execute.
oneOf:
  - type: string
  - $ref: "./when_cond.yaml"
  - type: array
    items:
      $ref: "./when_cond.yaml"