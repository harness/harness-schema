title: OnFailure
description: Failure strategy configuration. Supports single value or array. Supports expressions.
oneOf:
  - $ref: ./on-failure-config.yaml
  - type: array
    items:
      $ref: ./on-failure-config.yaml
  - $ref: ./expression.yaml
$schema: http://json-schema.org/draft-07/schema#

