title: Matrix
type: object
description: Defines a matrix execution strategy.

properties:
  axis:
    type: object
    additionalProperties:
      type: array
      items:
        type: string
  concurrency:
    description: Defines the matrix concurrency.
    type: number
  nodeName:
    description: Defines the naming of nodes for the matrix execution.
    type: string
  include:
    type: array
    items:
      type: object
      additionalProperties:
        type: string
  exclude:
    type: array
    items:
      type: object
      additionalProperties:
        type: string