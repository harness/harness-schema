title: Inputs
type: object
description: inputs to be used for entities
additionalProperties:
  anyOf:
    - string
    - array
    - number
    - object
    - boolean
propertyNames:
  pattern: "^[a-zA-Z_][0-9a-zA-Z_\\.$-]{0, 127}$"