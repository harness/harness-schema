title: StepBitriseSpec
type: object

properties:
  uses:
    type: string
  with:
    type: object
  envs:
    type: object
    additionalProperties:
      type: string
    description: The stage environment variables.
  outputs:
    type: array
    items:
      type: string
  mount:
    type: array
    items:
      $ref: ../common/mount.yaml