title: Attestation
type: object
properties:
  type:
    type: string
    enum:
      - cosign
      - keyless
      - keybased
      - secret-manager
  description:
    desc: This is the description for Attestation
  spec:
    type: object
$schema: http://json-schema.org/draft-07/schema#
allOf:
  - if:
      properties:
        type:
          const: cosign
    then:
      properties:
        spec:
          type: object
          oneOf:
            - $ref: cosign-attestation.yaml
            - $ref: cosign-secret-manager-attestation.yaml
  - if:
      properties:
        type:
          const: keyless
    then:
      properties:
        spec:
          type: object
          $ref: keyless-attestation.yaml
  - if:
      properties:
        type:
          const: keybased
    then:
      properties:
        spec:
          type: object
          $ref: cosign-attestation.yaml
  - if:
      properties:
        type:
          const: secret-manager
    then:
      properties:
        spec:
          type: object
          $ref: cosign-secret-manager-attestation.yaml
additionalProperties: false
