title: TasManifest
allOf:
- $ref: manifest-attributes.yaml
- type: object
  properties:
    auto_scaler:
      oneOf:
      - type: array
        items:
          type: string
      - type: string
      description: auto scaler path
    cf_cli_version:
      type: string
      enum:
      - V7
    metadata:
      type: string
    store:
      $ref: store-config-wrapper.yaml
    vars:
      oneOf:
      - type: array
        items:
          type: string
      - type: string
      description: vars paths
$schema: http://json-schema.org/draft-07/schema#