title: Stage Event Notification
type: object
required:
  - type
properties:
  type:
    type: string
    enum:
      - start
      - success
      - fail
      - all
    refs:
      type: array
      items:
        type: string
