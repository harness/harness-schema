title: StageEventNotification
type: object
required:
  - type
properties:
  type:
    type: string
    enum:
      - start
      - success
      - failed
      - all
    refs:
      type: array
      description: stage identifiers from the pipeline
      items:
        type: string
