title: Status
type: object

properties:
  disabled:
    type: boolean
  level:
    type: string
    enum:
      - pipeline
      - stage
  matrix:
    type: string
    enum:
      - itemize
      - summarize
  name:
    type: string