title: StepBitrise
type: object

properties:
  uses:
    type: string
  with:
    type: object
  # connector:
  #   type: string
  envs:
    type: object
    additionalProperties:
      type: string
    description: The stage environment variables.
  outputs:
    type: array
    items:
      type: string
  # network:
  #   type: string
  # resources:
  #   $ref: ./resources.yaml
  mount:
    type: array
    items:
      $ref: ./mount.yaml