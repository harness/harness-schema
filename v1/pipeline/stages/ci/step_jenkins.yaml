title: StepJenkins
type: object

properties:
  envs:
    type: object
    additionalProperties:
      type: string
    description: The stage environment variables.
  path:
    type: string
  mirror:
    type: string
  plugins:
    type: array
    items:
      type: string
  # network:
  #   type: string
  # resources:
  #   $ref: ./resources.yaml
  mount:
    type: array
    items:
      $ref: ./mount.yaml