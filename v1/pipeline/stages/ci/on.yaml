title: 'On'
type: object
properties:
  failure:
    oneOf:
      - $ref: "./failure.yaml"
      - type: array
        items:
          $ref: "./failure.yaml"

# skip and manually create go file
x-go-skip: true