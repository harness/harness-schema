title: ParallelStages
description: Parallel stages execution configuration.
type: object
required:
  - parallel
properties:
  parallel:
    description: Stages to execute in parallel.
    type: object
    required:
      - stages
    properties:
      stages:
        type: array
        items:
          $ref: ./stage-items.yaml
        minItems: 1
$schema: http://json-schema.org/draft-07/schema#

