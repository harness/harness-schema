# A runtime input field depends on other runtime input value field and fixed value field

version: 1
kind: pipeline
spec:
  stages:
    - name: test
      type: custom
      spec:
        steps:
          - name: JiraCreate_1
            type: jira-create
            id: JiraCreate_1
            spec:
              connectorRef: dummy-connectorRef
              projectKey: <+inputs.input-1>
              issueType: <+inputs.input-2>
              fields: []
            timeout: 10m
      tags: {}
  inputs:
    input-1:
      type: string
    input-2:
      type: string