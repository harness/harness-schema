version: 1
kind: trigger
spec:
  type: webhook
  spec:
    type: github
    spec:
      type: pull-request
      spec:
        actions:
          - open
          - close
        connector: github_connector