version: 1
kind: trigger
type: webhook
spec:
  type: github
  spec:
    type: pr
    spec:
      actions:
        - open
        - close
      connector: github_connector
  interval: 10m
  webhook: web_1
  input_set_refs:
    - is1
  execute_stages:
    - s1