version: 1
kind: trigger
spec:
  type: manifest
  spec:
    type: helm-chart
    spec:
      chart_name: chart1
      helm_version: v3
      store:
        type: http
        spec:
          connector: http_connector