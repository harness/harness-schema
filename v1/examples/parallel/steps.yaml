version: 1
kind: pipeline
spec:
  stages:
    name: custom
    type: custom
    spec:
      steps:
        - name: parallelSteps
          type: parallel
          spec:
            steps:
              - name: http
                type: http
                desc: my http step
                id: http1
                spec:
                  url: https://www.google.com
                  method: GET
                  input_vars: []
                  headers: []
                  output_vars: []
                  body:
                timeout: 10m
              - name: http
                type: http
                desc: my http step
                id: http2
                spec:
                  url: https://www.google.com
                  method: GET
                  input_vars: []
                  headers: []
                  output_vars: []
                  body:
                timeout: 10m