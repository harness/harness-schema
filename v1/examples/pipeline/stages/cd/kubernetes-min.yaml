version: 1
kind: pipeline
spec:
  stages:
    - type: deployment
      failure:
        - errors:
            - all
          action:
            type: fail
      spec:
        service:
          serviceRef: test
        environment:
          environmentRef: dev
          deployToAll: false
          infrastructureDefinitions:
            - identifier: aks
        deployment: kubernetes
        steps:
          - type: k8s_rolling_deploy
            spec: {}
