version: 1
kind: template
spec:
  type: step
  ref: temp1@v2
  spec:
    type: plugin
    timeout: 10m
    spec:
      uses: http
      with:
        url: http://<+template.inputs.env>.harness.io
        method: GET
  
  inputs:
    env:
      type: string