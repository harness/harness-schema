version: 1
template:
  type: Step
  versionLabel: v2
  spec:
    type: plugin
    timeout: 10m
    spec:
      uses: http
      with:
        url: http://<+template.inputs.env>.harness.io
        method: GET
  
  inputs:
    env:
      type: string