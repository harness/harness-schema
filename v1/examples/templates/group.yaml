version: 1
kind: template
spec:
  type: group
  stage: custom
  spec:
    steps:
      - type: http
        id: http_1
        spec:
          url: https://www.google.com
          method: GET
      - type: shell-script
        id: shell_script_1
        spec:
          shell: bash
          on_delegate: true
          source:
            type: Inline
            spec:
              script: echo Hi