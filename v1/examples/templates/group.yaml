version: 1
kind: template
spec:
  type: group
  spec:
    steps:
      - type: plugin
        id: http1
        timeout: 10m
        spec:
          uses: http
          with:
            url: https://www.<+ template.inputs.env>.harness.io
            method: GET