type: object
title: MonitoredServiceSources
properties:
  healthSources:
    type: array
    items:
      "$ref": "./health-source.yaml"
  changeSources:
    type: array
    items:
      "$ref": "./change-source.yaml"
# NOTE: no additionalProperties:false — new source families must not fail validation.
