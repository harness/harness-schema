title: system_event_trigger
allOf:
  - "$ref": "../trigger_spec.yaml"
  - type: object
    required:
      - type
      - spec
    properties:
      type:
        type: string
        enum:
          - Pipeline
  - if:
      properties:
        type:
          const: Pipeline
    then:
      properties:
        spec:
          "$ref": "../system_event_trigger/pipeline_system_event_spec.yaml"
"$schema": http://json-schema.org/draft-07/schema#
