title: nexus
allOf:
  - "$ref": "./artifact_type_spec.yaml"
  - type: object
    properties:
      artifactId:
        type: string
      classifier:
        type: string
      connectorRef:
        type: string
      eventConditions:
        type: array
        items:
          "$ref": "../trigger_event_data.yaml"
      extension:
        type: string
      groupId:
        type: string
      jexlCondition:
        type: string
      metaDataConditions:
        type: array
        items:
          "$ref": "../trigger_event_data.yaml"
      packageName:
        type: string
      repositoryFormat:
        type: string
      repositoryName:
        type: string
      repositoryUrl:
        type: string
      tag:
        type: string
"$schema": http://json-schema.org/draft-07/schema#
