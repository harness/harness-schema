title: gar_spec
allOf:
  - "$ref": "./artifact_type_spec.yaml"
  - type: object
    properties:
      connectorRef:
        type: string
      eventConditions:
        type: array
        items:
          "$ref": "../trigger_event_data.yaml"
      jexlCondition:
        type: string
      metaDataConditions:
        type: array
        items:
          "$ref": "../trigger_event_data.yaml"
      pkg:
        type: string
      project:
        type: string
      region:
        type: string
      repositoryName:
        type: string
      version:
        type: string
"$schema": http://json-schema.org/draft-07/schema#
