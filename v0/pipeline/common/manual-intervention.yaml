title: ManualIntervention
type: object
required:
  - timeout
  - onTimeout
properties:
  timeout:
    type: string
    format: duration
    example: [5s, 10s, 20s]
  onTimeout:
    properties:
      action:
        $ref: ./on-timeout.yaml
$schema: http://json-schema.org/draft-07/schema#