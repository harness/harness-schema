title: VMInfra
allOf:
- $ref: step-group-infra.yaml
- type: object
  required:
  - spec
  - type
  properties:
    spec:
      $ref: ../steps/custom/vm-infra-spec.yaml
    type:
      type: string
      enum:
      - VM
$schema: http://json-schema.org/draft-07/schema#
properties:
  description:
    desc: This is the description for VMInfra
