title: StepTemplateRef
# Same shape as TemplateLinkConfig (templateRef, versionLabel, label, templateInputs, …).
# Defined once in template-link-config.yaml so new fields only need editing there.
allOf:
  - $ref: ../../steps/custom/template-link-config.yaml
$schema: http://json-schema.org/draft-07/schema#
