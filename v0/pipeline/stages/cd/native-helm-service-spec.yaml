title: NativeHelmServiceSpec
allOf:
- $ref: service-spec.yaml
- type: object
  properties:
    hooks:
      type: array
      items:
        $ref: service-hook-wrapper.yaml
    manifestConfigurations:
      $ref: manifest-configurations.yaml
$schema: http://json-schema.org/draft-07/schema#
properties:
  description:
    desc: This is the description for NativeHelmServiceSpec
