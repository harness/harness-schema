title: AiAgentPlatform
type: object
required:
- type
- spec
properties:
  type:
    type: string
    enum:
    - AwsAgentCore
    - GoogleAgentRuntime
  description:
    desc: This is the description for AiAgentPlatform
$schema: http://json-schema.org/draft-07/schema#
allOf:
- if:
    properties:
      type:
        const: AwsAgentCore
  then:
    properties:
      spec:
        $ref: aws-agent-core-platform-spec.yaml
- if:
    properties:
      type:
        const: GoogleAgentRuntime
  then:
    properties:
      spec:
        $ref: google-agent-runtime-platform-spec.yaml
