title: AwsCoreAgentSource
type: object
required:
- type
- spec
properties:
  type:
    type: string
    enum:
    - container
    - s3
  description:
    desc: This is the description for AwsCoreAgentSource
$schema: http://json-schema.org/draft-07/schema#
allOf:
- if:
    properties:
      type:
        const: container
  then:
    properties:
      spec:
        $ref: container-agent-source.yaml
- if:
    properties:
      type:
        const: s3
  then:
    properties:
      spec:
        $ref: s3-agent-source.yaml
