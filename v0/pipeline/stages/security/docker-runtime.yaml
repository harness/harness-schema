title: DockerRuntime
allOf:
- $ref: runtime.yaml
- type: object
  required:
  - spec
  - type
  properties:
    spec:
      $ref: docker-runtime-spec.yaml
    type:
      type: string
      enum:
      - Docker
$schema: http://json-schema.org/draft-07/schema#
properties:
  description:
    desc: This is the description for DockerRuntime
