title: ContainerVMInfra
allOf:
- $ref: container-step-infra.yaml
- type: object
  required:
  - spec
  - type
  properties:
    spec:
      $ref: container-vm-infra-yaml-spec.yaml
    type:
      type: string
      enum:
      - KubernetesDirect
      - VM
$schema: http://json-schema.org/draft-07/schema#
properties:
  description:
    desc: This is the description for ContainerVMInfra
