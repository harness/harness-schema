title: GcrSbomSource
allOf:
  - $ref: sbom-source-spec.yaml
  - type: object
    required:
      - connector
      - host
      - project_id
      - image
    properties:
      connector:
        type: string
      host:
        type: string
      project_id:
        type: string
      image:
        type: string    
$schema: http://json-schema.org/draft-07/schema#
properties:
  description:
    desc: Gcr sbom source spec
