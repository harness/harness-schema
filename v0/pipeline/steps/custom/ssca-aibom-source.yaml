title: SscaAibomSource
type: object
required:
- type
properties:
  type:
    type: string
    enum:
    - repository
  description:
    desc: This is the description for SscaAibomSource
$schema: http://json-schema.org/draft-07/schema#
allOf:
- if:
    properties:
      type:
        const: repository
  then:
    properties:
      spec:
        $ref: ssca-aibom-repository-source.yaml
