title: ContainerVMInfraYamlSpec
type: object
required:
- poolName
properties:
  harnessImageConnectorRef:
    type: string
  poolName:
    type: string
  os:
    oneOf:
    - type: string
      enum:
      - Linux
      - MacOS
      - Windows
    - type: string
      pattern: ^<\+input>((\.)((executionInput\(\))|(allowedValues|selectOneFrom|selectManyFrom|default|regex)\(.+?\)))*$
      minLength: 1
  initTimeout:
    type: string
  resources:
    $ref: ../../common/container-resource.yaml
  description:
    desc: This is the description for ContainerVMInfraYamlSpec
$schema: http://json-schema.org/draft-07/schema#
