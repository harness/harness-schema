title: AcrSbomSource
allOf:
  - $ref: sbom-source-spec.yaml
  - type: object
    required:
      - image
      - connector
    properties:
      connector:
        type: string
      image:
        type: string
      subscription_id:
        type: string
$schema: http://json-schema.org/draft-07/schema#
properties:
  description:
    desc: Acr sbom source spec