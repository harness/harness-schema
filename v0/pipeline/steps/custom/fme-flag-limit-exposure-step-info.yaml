title: FmeFlagLimitExposureStepInfo
allOf:
  - $ref: ../../common/step-spec-type.yaml
  - type: object
    required:
      - flagName
      - environment
      - limit
    properties:
      flagName:
        oneOf:
          - $ref: ../common/fme-flag-common-flag-name.yaml
          - $ref: ../common/common-jexl.yaml
      environment:
        oneOf:
          - $ref: ../common/fme-flag-common-environment.yaml
          - $ref: ../common/common-jexl.yaml
      limit:
        description: "Exposure limit"
        oneOf:
          - type: integer
            minimum: 0
            maximum: 100
          - $ref: ../common/common-jexl.yaml
$schema: http://json-schema.org/draft-07/schema#
