title: TemplateLinkConfig
type: object
required:
- templateRef
properties:
  templateInputs:
    $ref: ../../common/json-node.yaml
  templateOverrides:
    $ref: ../../common/json-node.yaml
  templateRef:
    type: string
  templateVariables:
    $ref: ../../common/json-node.yaml
  versionLabel:
    type: string
    pattern: ^[0-9a-zA-Z][^\s/&]{0,63}$
  label:
    type: string
    description: Optional template label in identifier format (case-insensitive; letters, digits, underscore; must start with letter or underscore).
    maxLength: 128
    pattern: ^[a-zA-Z_][0-9a-zA-Z_]{0,127}$
  description:
    desc: This is the description for TemplateLinkConfig
$schema: http://json-schema.org/draft-07/schema#
