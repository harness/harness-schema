title: UpdateReleaseRepoStepInfo
allOf:
- $ref: ../../common/step-spec-type.yaml
- type: object
  properties:
    delegateSelectors:
      oneOf:
      - type: array
        items:
          type: string
      - type: string
        pattern: ^<\+input>((\.)((executionInput\(\))|(allowedValues|default|regex)\(.+?\)))*$
        minLength: 1
    prTitle:
      type: string
    stringMap:
      $ref: parameter-field-map-string-string.yaml
    variables:
      type: array
      items:
        oneOf:
        - $ref: ../../common/number-n-g-variable.yaml
        - $ref: ../../common/secret-n-g-variable.yaml
        - $ref: ../../common/string-n-g-variable.yaml
$schema: http://json-schema.org/draft-07/schema#
type: object
properties:
  delegateSelectors:
    oneOf:
    - type: array
      items:
        type: string
    - type: string
      pattern: ^<\+input>((\.)((executionInput\(\))|(allowedValues|default|regex)\(.+?\)))*$
      minLength: 1
  prTitle:
    type: string
  stringMap:
    $ref: parameter-field-map-string-string.yaml
  variables:
    type: array
    items:
      oneOf:
      - $ref: ../../common/number-n-g-variable.yaml
      - $ref: ../../common/secret-n-g-variable.yaml
      - $ref: ../../common/string-n-g-variable.yaml
  description:
    desc: This is the description for UpdateReleaseRepoStepInfo
