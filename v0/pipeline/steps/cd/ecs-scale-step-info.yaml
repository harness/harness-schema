title: EcsScaleStepInfo
allOf:
- $ref: ../../common/step-spec-type.yaml
- type: object
  required:
  - service
  - instanceUnit
  - instances
  properties:
    connectorRef:
      description: AWS connector reference
      oneOf:
      - $ref: ../common/string-without-jexl.yaml
      - $ref: ../common/common-jexl.yaml
    region:
      description: AWS region
      oneOf:
      - $ref: ../common/string-without-jexl.yaml
      - $ref: ../common/common-jexl.yaml
    cluster:
      description: ECS cluster name
      oneOf:
      - $ref: ../common/string-without-jexl.yaml
      - $ref: ../common/common-jexl.yaml
    service:
      description: ECS service name
      oneOf:
      - $ref: ../common/string-without-jexl.yaml
      - $ref: ../common/common-jexl.yaml
    instanceUnit:
      description: Instance unit type (COUNT or PERCENTAGE)
      type: string
      enum:
      - COUNT
      - PERCENTAGE
    instances:
      description: Number of instances (count or percentage)
      oneOf:
      - type: integer
        minimum: 0
      - $ref: ../common/common-jexl.yaml
    skipSteadyStateCheck:
      description: Skip waiting for service to reach steady state
      oneOf:
      - type: boolean
      - $ref: ../common/common-jexl.yaml
    delegateSelectors:
      oneOf:
      - type: array
        items:
          type: string
      - type: string
        pattern: (<\+.+>.*)
        minLength: 1
$schema: http://json-schema.org/draft-07/schema#
type: object
required:
- service
- instanceUnit
- instances
properties:
  connectorRef:
    description: AWS connector reference
    oneOf:
    - $ref: ../common/string-without-jexl.yaml
    - $ref: ../common/common-jexl.yaml
  region:
    description: AWS region
    oneOf:
    - $ref: ../common/string-without-jexl.yaml
    - $ref: ../common/common-jexl.yaml
  cluster:
    description: ECS cluster name
    oneOf:
    - $ref: ../common/string-without-jexl.yaml
    - $ref: ../common/common-jexl.yaml
  service:
    description: ECS service name
    oneOf:
    - $ref: ../common/string-without-jexl.yaml
    - $ref: ../common/common-jexl.yaml
  instanceUnit:
    description: Instance unit type (COUNT or PERCENTAGE)
    type: string
    enum:
    - COUNT
    - PERCENTAGE
  instances:
    description: Number of instances (count or percentage)
    oneOf:
    - type: integer
      minimum: 0
    - $ref: ../common/common-jexl.yaml
  skipSteadyStateCheck:
    description: Skip waiting for service to reach steady state
    oneOf:
    - type: boolean
    - $ref: ../common/common-jexl.yaml
  delegateSelectors:
    oneOf:
    - type: array
      items:
        type: string
    - type: string
      pattern: (<\+.+>.*)
      minLength: 1
  description:
    desc: This is the description for EcsScaleStepInfo
