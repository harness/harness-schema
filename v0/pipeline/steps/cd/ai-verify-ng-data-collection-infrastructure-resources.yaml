title: DataCollectionInfrastructureResources
description: Optional resource requests/limits for AI Verify data collection infrastructure. Omit the block entirely,
  or include only the fields you need; CPU and memory are optional under requests and limits.
type: object
properties:
  limits:
    $ref: ../../common/limits.yaml
  requests:
    $ref: ../../common/limits.yaml
$schema: http://json-schema.org/draft-07/schema#
