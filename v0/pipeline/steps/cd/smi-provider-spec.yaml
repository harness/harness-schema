title: SMIProvider
type: object
required:
  - rootService
  - routes
  - destinations
properties:
  rootService:
    type: string
  routes:
    type: array
    items:
      "$ref": "route-spec.yaml"
  destinations:
    type: array
    items:
      "$ref": "traffic-routing-destination.yaml"
$schema: http://json-schema.org/draft-07/schema#
