title: GCEProvisionBackendServiceStepInfo
allOf:
  - $ref: ../../common/step-spec-type.yaml
  - type: object
    required:
      - project
      - backendServiceName
    properties:
      connectorRef:
        type: string
      image:
        type: string
      imagePullPolicy:
        oneOf:
          - type: string
            enum:
              - Always
              - Never
              - IfNotPresent
          - type: string
            pattern: (<\+.+>.*)
            minLength: 1
      privileged:
        oneOf:
          - type: boolean
          - type: string
            pattern: ^<\+input>((\.)((executionInput\(\))|(allowedValues|selectOneFrom|selectManyFrom|default|regex)\(.+?\)))*$
            minLength: 1
      resources:
        $ref: ../../common/container-resource.yaml
      runAsUser:
        oneOf:
          - type: integer
            format: int32
          - type: string
      preExecution:
        type: string
      project:
        type: string
      region:
        type: string
      backendServiceName:
        type: string
      gcpConnectorRef:
        type: string
      configFilePath:
        type: string
      delegateSelectors:
        oneOf:
          - type: array
            items:
              type: string
          - type: string
            pattern: (<\+.+>.*)
            minLength: 1
$schema: http://json-schema.org/draft-07/schema#
type: object
properties:
  connectorRef:
    type: string
  image:
    type: string
  imagePullPolicy:
    oneOf:
      - type: string
        enum:
          - Always
          - Never
          - IfNotPresent
      - type: string
        pattern: (<\+.+>.*)
        minLength: 1
  privileged:
    oneOf:
      - type: boolean
      - type: string
        pattern: ^<\+input>((\.)((executionInput\(\))|(allowedValues|selectOneFrom|selectManyFrom|default|regex)\(.+?\)))*$
        minLength: 1
  resources:
    $ref: ../../common/container-resource.yaml
  runAsUser:
    oneOf:
      - type: integer
        format: int32
      - type: string
  preExecution:
    type: string
  project:
    type: string
  region:
    type: string
  backendServiceName:
    type: string
  gcpConnectorRef:
    type: string
  configFilePath:
    type: string
  delegateSelectors:
    oneOf:
      - type: array
        items:
          type: string
      - type: string
        pattern: (<\+.+>.*)
        minLength: 1
  description:
    desc: This is the description for GCEProvisionBackendServiceStepInfo
