title: JiraReleaseStepInfo
allOf:
  - $ref: ../../common/step-spec-type.yaml
  - type: object
    properties:
      entityRef:
        type: string
        pattern: (<\+.+>.*)
      delegateSelectors:
        oneOf:
          - type: array
            items:
              type: string
          - type: string
            pattern: (<\+.+>.*)
      issueKeys:
        oneOf:
          - type: array
            items:
              type: string
          - type: string
            pattern: (<\+.+>.*)
$schema: http://json-schema.org/draft-07/schema#
type: object
properties:
  entityRef:
    type: string
    pattern: (<\+.+>.*)
  delegateSelectors:
    oneOf:
      - type: array
        items:
          type: string
      - type: string
        pattern: (<\+.+>.*)
  issueKeys:
    oneOf:
      - type: array
        items:
          type: string
      - type: string
        pattern: (<\+.+>.*)
  description:
    desc: This is the description for JiraReleaseStepInfo
