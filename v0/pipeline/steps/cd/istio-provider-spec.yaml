title: IstioProvider
type: object
required:
  - routes
  - destinations
properties:
  gateways:
    type: array
    items:
      type: string
  hosts:
    type: array
    items:
      type: string
  routes:
    type: array
    items:
      "$ref": "route-spec.yaml"
  destinations:
    type: array
    items:
      "$ref": "traffic-routing-destination.yaml"
$schema: http://json-schema.org/draft-07/schema#
