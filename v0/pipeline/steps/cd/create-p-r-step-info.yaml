title: CreatePRStepInfo
allOf:
- $ref: ../../common/step-spec-type.yaml
- type: object
  required:
  - overrideConfig
  - shell
  properties:
    delegateSelectors:
      oneOf:
      - type: array
        items:
          type: string
      - type: string
        pattern: ^<\+input>((\.)((executionInput\(\))|(allowedValues|default|regex)\(.+?\)))*$
        minLength: 1
    overrideConfig:
      oneOf:
      - type: boolean
      - type: string
    shell:
      type: string
      enum:
      - Bash
    source:
      $ref: create-p-r-step-update-config-script-wrapper.yaml
    stringMap:
      $ref: ../common/parameter-field-map-string-string.yaml
$schema: http://json-schema.org/draft-07/schema#
type: object
required:
- overrideConfig
- shell
properties:
  delegateSelectors:
    oneOf:
    - type: array
      items:
        type: string
    - type: string
      pattern: ^<\+input>((\.)((executionInput\(\))|(allowedValues|default|regex)\(.+?\)))*$
      minLength: 1
  overrideConfig:
    oneOf:
    - type: boolean
    - type: string
  shell:
    type: string
    enum:
    - Bash
  source:
    $ref: create-p-r-step-update-config-script-wrapper.yaml
  stringMap:
    $ref: ../common/parameter-field-map-string-string.yaml
  description:
    desc: This is the description for CreatePRStepInfo
