title: ContainerK8sInfra
allOf:
- $ref: container-step-infra.yaml
- type: object
  required:
  - spec
  - type
  properties:
    spec:
      $ref: container-infra-yaml-spec.yaml
    type:
      type: string
      enum:
      - KubernetesDirect
$schema: http://json-schema.org/draft-07/schema#
properties:
  description:
    desc: This is the description for ContainerK8sInfra
