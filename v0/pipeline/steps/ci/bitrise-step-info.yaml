title: BitriseStepInfo
allOf:
- $ref: ci-step-info.yaml
- type: object
  required:
  - uses
  properties:
    env:
      oneOf:
      - $ref: ../common/parameter-field-map-string-string.yaml
      - type: string
    uses:
      type: string
    with:
      oneOf:
      - $ref: ../common/parameter-field-map-string-string.yaml
      - type: string
$schema: http://json-schema.org/draft-07/schema#
type: object
required:
- uses
properties:
  env:
    oneOf:
    - $ref: ../common/parameter-field-map-string-string.yaml
    - type: string
  uses:
    type: string
  with:
    oneOf:
    - $ref: ../common/parameter-field-map-string-string.yaml
    - type: string
  description:
    desc: This is the description for BitriseStepInfo
