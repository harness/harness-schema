title: GarSourceSpec
allOf:
- $ref: provenance-source-spec.yaml
- type: object
  required:
  - connector
  - host
  - projectId
  - image
  - artifact_digest
  properties:
    connector:
      type: string
    host:
      type: string
    projectId:
      type: string
    image:
      type: string
    artifact_digest:
      type: string
    tags:
      type: array
      items:
        type: string
$schema: http://json-schema.org/draft-07/schema#
properties:
  description:
    desc: Gar source spec