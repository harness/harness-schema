title: VmPoolYaml
allOf:
- $ref: vm-infra-spec.yaml
- type: object
  required:
  - spec
  - type
  properties:
    spec:
      $ref: vm-pool-yaml-spec.yaml
    type:
      type: string
      enum:
      - Pool
$schema: http://json-schema.org/draft-07/schema#
properties:
  description:
    desc: This is the description for VmPoolYaml
