title: CustomApprovalStepInfo
allOf:
- $ref: step-spec-type.yaml
- type: object
  required:
  - approvalCriteria
  - retryInterval
  - scriptTimeout
  - shell
  - source
  properties:
    approvalCriteria:
      $ref: criteria-spec-wrapper.yaml
    delegateSelectors:
      oneOf:
      - type: array
        items:
          type: string
      - type: string
        pattern: ^<\+input>((\.)((executionInput\(\))|(allowedValues|default|regex)\(.+?\)))*$
        minLength: 1
    environmentVariables:
      type: array
      items:
        oneOf:
        - $ref: number-n-g-variable.yaml
        - $ref: secret-n-g-variable.yaml
        - $ref: string-n-g-variable.yaml
    outputVariables:
      type: array
      items:
        oneOf:
        - $ref: number-n-g-variable.yaml
        - $ref: secret-n-g-variable.yaml
        - $ref: string-n-g-variable.yaml
    rejectionCriteria:
      $ref: criteria-spec-wrapper.yaml
    retryInterval:
      type: string
      pattern: ^(([1-9])+\d+[s])|(((([1-9])+\d*[mhwd])+([\s]?\d+[smhwd])*)|(<\+input>.*)|(.*<\+.*>.*)|(^$))$
    scriptTimeout:
      type: string
      pattern: ^(([1-9])+\d+[s])|(((([1-9])+\d*[mhwd])+([\s]?\d+[smhwd])*)|(<\+input>.*)|(.*<\+.*>.*)|(^$))$
    shell:
      type: string
      enum:
      - Bash
      - PowerShell
    source:
      $ref: shell-script-source-wrapper.yaml
$schema: http://json-schema.org/draft-07/schema#
type: object
required:
- approvalCriteria
- retryInterval
- scriptTimeout
- shell
- source
properties:
  approvalCriteria:
    $ref: criteria-spec-wrapper.yaml
  delegateSelectors:
    oneOf:
    - type: array
      items:
        type: string
    - type: string
      pattern: ^<\+input>((\.)((executionInput\(\))|(allowedValues|default|regex)\(.+?\)))*$
      minLength: 1
  environmentVariables:
    type: array
    items:
      oneOf:
      - $ref: number-n-g-variable.yaml
      - $ref: secret-n-g-variable.yaml
      - $ref: string-n-g-variable.yaml
  outputVariables:
    type: array
    items:
      oneOf:
      - $ref: number-n-g-variable.yaml
      - $ref: secret-n-g-variable.yaml
      - $ref: string-n-g-variable.yaml
  rejectionCriteria:
    $ref: criteria-spec-wrapper.yaml
  retryInterval:
    type: string
    pattern: ^(([1-9])+\d+[s])|(((([1-9])+\d*[mhwd])+([\s]?\d+[smhwd])*)|(<\+input>.*)|(.*<\+.*>.*)|(^$))$
  scriptTimeout:
    type: string
    pattern: ^(([1-9])+\d+[s])|(((([1-9])+\d*[mhwd])+([\s]?\d+[smhwd])*)|(<\+input>.*)|(.*<\+.*>.*)|(^$))$
  shell:
    type: string
    enum:
    - Bash
    - PowerShell
  source:
    $ref: shell-script-source-wrapper.yaml
