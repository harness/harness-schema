title: ArtifactSigningAcrSourceSpec
allOf:
  - $ref: artifact-signing-source-spec.yaml
  - type: object
    required:
      - connector
      - image
      - digest
    properties:
      connector:
        type: string
      image:
        type: string
      subscriptionId:
        type: string
      digest:
        type: string
$schema: http://json-schema.org/draft-07/schema#
properties:
  description:
    desc: This is the description for ArtifactSigningAcrSourceSpec