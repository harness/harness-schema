title: StepWhenCondition
type: object
required:
- stageStatus
properties:
  condition:
    type: string
    pattern: ^(?!<\+input>.*\.executionInput\(\)(.*)$)
  stageStatus:
    type: string
    enum:
    - Success
    - Failure
    - All
$schema: http://json-schema.org/draft-07/schema#
