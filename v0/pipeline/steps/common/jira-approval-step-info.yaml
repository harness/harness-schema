title: JiraApprovalStepInfo
allOf:
- $ref: step-spec-type.yaml
- type: object
  required:
  - approvalCriteria
  - connectorRef
  - issueKey
  properties:
    approvalCriteria:
      $ref: criteria-spec-wrapper.yaml
    connectorRef:
      type: string
    delegateSelectors:
      oneOf:
      - type: array
        items:
          type: string
      - type: string
        pattern: ^<\+input>((\.)((executionInput\(\))|(allowedValues|default|regex)\(.+?\)))*$
        minLength: 1
    issueKey:
      type: string
    issueType:
      type: string
    projectKey:
      type: string
    rejectionCriteria:
      $ref: criteria-spec-wrapper.yaml
$schema: http://json-schema.org/draft-07/schema#
type: object
required:
- approvalCriteria
- connectorRef
- issueKey
properties:
  approvalCriteria:
    $ref: criteria-spec-wrapper.yaml
  connectorRef:
    type: string
  delegateSelectors:
    oneOf:
    - type: array
      items:
        type: string
    - type: string
      pattern: ^<\+input>((\.)((executionInput\(\))|(allowedValues|default|regex)\(.+?\)))*$
      minLength: 1
  issueKey:
    type: string
  issueType:
    type: string
  projectKey:
    type: string
  rejectionCriteria:
    $ref: criteria-spec-wrapper.yaml
