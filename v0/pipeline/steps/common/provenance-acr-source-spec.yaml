title: AcrSourceSpec
allOf:
  - $ref: provenance-source-spec.yaml
  - type: object
    required:
      - connector
      - repository
      - artifact_digest
    properties:
      connector:
        type: string
      repository:
        type: string
      subscriptionId:
        type: string
      artifact_digest:
        type: string
$schema: http://json-schema.org/draft-07/schema#
properties:
  description:
    desc: Acr source spec