title: AcrSourceSpec
allOf:
  - $ref: provenance-source-spec.yaml
  - type: object
    required:
      - connector
      - repository
      - digest
    properties:
      connector:
        type: string
      repository:
        type: string
      subscriptionId:
        type: string
      digest:
        type: string
      tags:
        type: array
        items:
          type: string
$schema: http://json-schema.org/draft-07/schema#
properties:
  description:
    desc: Acr source spec