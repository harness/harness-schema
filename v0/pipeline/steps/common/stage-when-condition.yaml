title: StageWhenCondition
type: object
required:
- pipelineStatus
properties:
  condition:
    type: string
    pattern: ^(?!<\+input>.*\.executionInput\(\)(.*)$)
  pipelineStatus:
    type: string
    enum:
    - Success
    - Failure
    - All
$schema: http://json-schema.org/draft-07/schema#
