title: SlsaVerificationStepInfo
allOf:
- $ref: ../../common/step-spec-type.yaml
- type: object
  properties:
    source:
      $ref: slsa-verification-source.yaml
    verify_attestation:
      $ref: slsa-verify-attestation.yaml
    resources:
      $ref: ../../common/container-resource.yaml
    overrideConnectorRef:
      type: string
    imageTag:
      type: string
    imagePullPolicy:
      oneOf:
      - type: string
        enum:
        - Always
        - Never
        - IfNotPresent
      - type: string
        pattern: (<\+.+>.*)
        minLength: 1
$schema: http://json-schema.org/draft-07/schema#
type: object
properties:
  source:
    $ref: slsa-verification-source.yaml
  verify_attestation:
    $ref: slsa-verify-attestation.yaml
  resources:
    $ref: ../../common/container-resource.yaml
  overrideConnectorRef:
    type: string
  imageTag:
    type: string
  imagePullPolicy:
    oneOf:
    - type: string
      enum:
      - Always
      - Never
      - IfNotPresent
    - type: string
      pattern: (<\+.+>.*)
      minLength: 1
  description:
    desc: This is the description for SlsaVerificationStepInfo
