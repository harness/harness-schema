title: QueueStepInfo
allOf:
- $ref: step-spec-type.yaml
- type: object
  required:
  - key
  - scope
  properties:
    key:
      type: string
    scope:
      type: string
      enum:
      - Pipeline
      - Stage
$schema: http://json-schema.org/draft-07/schema#
type: object
required:
- key
- scope
properties:
  key:
    type: string
  scope:
    type: string
    enum:
    - Pipeline
    - Stage
