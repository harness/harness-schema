title: EcrSourceSpec
allOf:
  - $ref: provenance-source-spec.yaml
  - type: object
    required:
      - image
      - digest
    properties:
      connector:
        type: string
      region:
        type: string
      account:
        type: string
      image:
        type: string
      artifact_digest:
        type: string
      tags:
        type: array
        items:
          type: string
$schema: http://json-schema.org/draft-07/schema#
properties:
  description:
    desc: Ecr source spec