title: EcrSourceSpec
allOf:
  - $ref: provenance-source-spec.yaml
  - type: object
    required:
      - image
      - digest
      - connector
    properties:
      connector:
        type: string
      region:
        type: string
      account:
        type: string
      image:
        type: string
      digest:
        type: string
$schema: http://json-schema.org/draft-07/schema#
properties:
  description:
    desc: Ecr source spec