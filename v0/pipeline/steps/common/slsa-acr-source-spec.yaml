title: SlsaAcrSourceSpec
allOf:
  - $ref: slsa-verification-source-spec.yaml
  - type: object
    required:
      - connector
      - image_path
      - tag
    properties:
      connector:
        type: string
      image_path:
        type: string
      subscription_id:
        type: string
      tag:
        type: string
$schema: http://json-schema.org/draft-07/schema#
properties:
  description:
    desc: Slsa Acr source spec