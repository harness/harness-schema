title: TemplateMonitoredServiceSpec
allOf:
- $ref: monitored-service-spec.yaml
- type: object
  required:
  - monitoredServiceTemplateRef
  - templateInputs
  - versionLabel
  properties:
    monitoredServiceTemplateRef:
      type: string
    templateInputs:
      $ref: ../../common/json-node.yaml
    versionLabel:
      type: string
    label:
      type: string
      description: Optional single-token label for template resolution (case-insensitive at runtime; ASCII letters, digits, underscore, hyphen only).
      maxLength: 50
      pattern: ^[A-Za-z0-9][A-Za-z0-9_-]{0,49}$
$schema: http://json-schema.org/draft-07/schema#
properties:
  description:
    desc: This is the description for TemplateMonitoredServiceSpec
